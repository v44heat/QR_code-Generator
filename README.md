# QR Code Generator

A simple Python application to generate QR codes from URLs.

## Installation
```bash
git clone https://github.com/v44heat/qr-code-generator.git
cd qr-code-generator
pip install -r requirements.txt

## Usage
```bash
python main.py

Enter URL when prompted and QR code will be saved as qrcode.png when you specify the file_path